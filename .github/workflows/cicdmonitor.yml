name: schedule_cron

on:
  schedule:
    - cron: "33 14 * * *"
      timezone: "Asia/Kolkata"

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: python app.py