name : python_push

on:
  push:
    branches:
      - main
    paths:
      - "*.py"

  workflow_dispatch:

jobs:
  job1:
    runs-on : ubuntu-latest
    steps:
      - uses : actions/checkout@v4
      - run : python app.py